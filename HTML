<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rollin' Rendezvous</title>

    <style>
            body {
                text-align: center;
                font-size: 20px;
                background-image: url('static/static/bgpic.png'); /* Relative path to the image */
                background-size: cover;
                background-position: center;
                background-repeat: 2;
                color: #FBF5D6;
}
        h1 {
            font-size: 36px;  /* Adjust the font size as needed */
            font-family: Lucky Bones, cursive;
            text-transform: uppercase;
            letter-spacing: 4px;
            text-shadow: 8px 8px 10px rgba(0, 0, 0, 0.3); /* 3D text effect */
        }

        p {
            font-size: 36px;  /* Adjust the font size as needed */
            text-shadow: 4px 4px 5px rgba(0, 0, 0, 0.2); /* 3D text effect */
        }

        img {
            width: 150px;  /* Adjust the image size as needed */
            height: 150px;
            margin: 20px;  /* Add some margin for spacing */
        }

        form {
            margin-top: 20px;  /* Add some margin for spacing */
        }

        #instructions {
            margin-top: 30px;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <h1>Rollin' Rendezvous</h1>
    <p>You rolled: {{ first_dice }} and {{ second_dice }}</p>
    <p>{{ result }}</p>

    <img src="{{ url_for('static', filename=dice1_img) }}" alt="Dice 1">
    <img src="{{ url_for('static', filename=dice2_img) }}" alt="Dice 2">

    {% if result %}
        <form action="/" method="post">
            <input type="submit" value="Roll Again">
        </form>
    {% endif %}

    <div id="instructions">
        <p>Instructions:</p>
        <p>Roll two dice, if both dice show odd numbers (1, 3, 5). You win!</p>
        <p>But, if you roll both even, odd and even, and even and odd. You lose!</p>
    </div>
</body>
</html>
